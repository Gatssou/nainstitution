function myFunction(x) {
    if (x.matches.matches){
      document.body.style.backgroundAttachment = "fixed";
    }else {
        document.body.style.backgroundAttachment = "fixed";
      }
    }
    let x = window.matchMedia("(max-width: 700px)")
    myFunction(x) 
    x.addEventListener(myFunction) 